apiVersion: v1
kind: Service
metadata:
  name: ${APP_NAME}-mysql
  namespace: ${KUBE_NAMESPACE}
  labels:
    app: ${APP_NAME}-mysql
spec:
  selector:
    app: ${APP_NAME}-mysql
  ports:
    - port: 3306
      targetPort: 3306
  type: ClusterIP
  clusterIP: None  # headless for StatefulSet DNS

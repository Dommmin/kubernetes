# k3s uses Traefik ingress by default.
# Requires: cert-manager installed on k3s cluster
#   kubectl apply -f https://github.com/cert-manager/cert-manager/releases/latest/download/cert-manager.yaml
#
# Create ClusterIssuer (once):
#   apiVersion: cert-manager.io/v1
#   kind: ClusterIssuer
#   metadata:
#     name: letsencrypt-prod
#   spec:
#     acme:
#       server: https://acme-v02.api.letsencrypt.org/directory
#       email: your@email.com
#       privateKeySecretRef:
#         name: letsencrypt-prod
#       solvers:
#         - http01:
#             ingress:
#               ingressClassName: traefik
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ${APP_NAME}-ingress
  namespace: ${KUBE_NAMESPACE}
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    traefik.ingress.kubernetes.io/router.entrypoints: "web,websecure"
    traefik.ingress.kubernetes.io/router.middlewares: "${KUBE_NAMESPACE}-redirect-https@kubernetescrd,${KUBE_NAMESPACE}-body-size@kubernetescrd"
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - laravel-test.shop
        - www.laravel-test.shop
        - admin.laravel-test.shop
      secretName: ${APP_NAME}-tls
  rules:
    - host: laravel-test.shop
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ${APP_NAME}-client
                port:
                  number: 3000
    - host: www.laravel-test.shop
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ${APP_NAME}-client
                port:
                  number: 3000
    - host: admin.laravel-test.shop
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ${APP_NAME}-server
                port:
                  number: 80

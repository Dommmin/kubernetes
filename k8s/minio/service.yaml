apiVersion: v1
kind: Service
metadata:
  name: ${APP_NAME}-minio
  namespace: ${KUBE_NAMESPACE}
spec:
  selector:
    app: ${APP_NAME}-minio
  ports:
    - name: api
      port: 9000
      targetPort: 9000
    - name: console
      port: 9001
      targetPort: 9001

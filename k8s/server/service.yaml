apiVersion: v1
kind: Service
metadata:
  name: ${APP_NAME}-server
  namespace: ${KUBE_NAMESPACE}
  labels:
    app: ${APP_NAME}-server
spec:
  selector:
    app: ${APP_NAME}-server
    component: web
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: ClusterIP
